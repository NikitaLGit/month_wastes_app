services:
  month-wastes-backend:
    build: ./backend
    container_name: month-wastes-backend
    restart: unless-stopped
    env_file: ./backend/.env
    ports:
      - "3003:3003"
    networks:
      - app_default

networks:
  app_default:
    external: true
